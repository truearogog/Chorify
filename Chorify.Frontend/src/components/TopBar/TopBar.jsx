import React from "react";
import "./topBar.css"

const TopBar = () => {
  return (
    <div class="top-bar">
      <div class="website-name">Chorify</div>
      <button class="logout-button">Log Out</button>
    </div>
  );
};

export default TopBar;
